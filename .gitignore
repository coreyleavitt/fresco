# Build artifacts
bin/
nimcache/
*.exe

# Docker dev home (mirrors recall's pattern — gitignored writable nimble cache)
.docker-home/

# Vendored chronos fork (gitignored; populated by hand-copy from
# ~/projects/chronos — same pattern as amoxtli vendoring fresco at lib/).
lib/

# Editor noise
.vscode/
.idea/
*.swp
.DS_Store

# Test temp dirs
/tmp/fresco-*

# Local-only env
.env
